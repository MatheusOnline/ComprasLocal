Componentes
    UI = Todos os elementos simples 
    Fetures = Conjunto de UI
    Layout  = Conjuntos de Features layout vai ter que ter skelton
    

    const loginMutation = useLogin();

const handleSubmit = () => {
  loginMutation.mutate({
    email,
    password,
  });
};

<Suspense fallback={<Skeleton />}>
  <CartSummary />
</Suspense>

const CartSummary = React.lazy(() => import("./CartSummary"));
const CatalogProducts = React.lazy(() => import("./CatalogProducts"));